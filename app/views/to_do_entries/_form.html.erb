<%= form_with(model: to_do_entry) do |form| %>
  <% if to_do_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(to_do_entry.errors.count, "error") %> prohibited this to_do_entry from being saved:</h2>

      <ul>
        <% to_do_entry.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
